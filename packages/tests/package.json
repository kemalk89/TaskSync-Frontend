{
  "name": "@times/tests",
  "version": "0.0.0",
  "scripts": {
    "test": "tsc --build && npm run test:headless",
    "test:headless": "tsc --build && cypress run",
    "test:openui": "tsc --build && cypress open",
    "build": "echo No build script for cypress tests project!"
  }
}