{
  "name": "times-ui",
  "version": "0.0.0",
  "description": "A monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "test:e2e": "npm run -w @times/tests test",
    "clean": "tsc --build --clean",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^7.2.0",
    "typescript": "^5.0.2"
  }
}
