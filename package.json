{
  "name": "times-ui",
  "version": "0.0.0",
  "description": "A monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w @times/app dev",
    "test:e2e": "npm run -w @times/tests test",
    "clean": "tsc --build --clean",
    "build": "tsc --build && npm run build -ws"
  }
}
